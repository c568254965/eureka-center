server:
  port: 6801


eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka
spring:
  profiles:
    active: dev

---
spring:
  profiles: test

eureka:
  instance:
    hostname: csrr-eureka
    prefer-ip-address: false

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://140.143.227.51:6802/eureka

---

server:
  port: 6802
spring:
  profiles: test2
eureka:
  instance:
    hostname: csrr-eureka    #名称，，注册要用
    prefer-ip-address: false
  client:
    register-with-eureka: true
    fetch-registry: true

    service-url:
      defaultZone: http://140.143.227.51:6801/eureka

